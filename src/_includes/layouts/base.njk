---
title: Jean-Rémy Praud - Developpeur fullstack
description: Tech Lead chez Dernier Cri, j’aime travailler sur des projets porteurs de sens. Je m’intéresse tout particulièrement à l’éco-conception et à l’accessibilité.
---

<!DOCTYPE html>
<html lang="fr-FR">
  <head>
    {% if page.url == "/" %}
      <title>{{ title }}</title>
    {% else %}
      <title>{{ title }} - Jean-Rémy Praud</title>
    {% endif %}
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="{{ description }}">
    <meta name="robots" content="noodp">
    {% favicon './src/assets/img/favicon.svg' %}

    <link rel="stylesheet" href="{{ '/assets/css/base.css' | url }}">
    <link rel="stylesheet" href="{{ '/assets/css/print.css' | url }}" media="print">
  </head>
  <body class="page">
    <div class="content container">

      {% renderTemplate "webc" %}
      <site-header></site-header>
      {% endrenderTemplate %}
      <main class="main">
        {{ content | safe }}
      </main>
      {% renderTemplate "webc" %}
      <site-footer></site-footer>
      {% endrenderTemplate %}
    </div>
    {% renderTemplate "webc" %}
    <site-sidebar></site-sidebar>
    {% endrenderTemplate %}
  </body>
</html>